function [ transformed ] = f_distance_transform( ContourSegment )
%f_distance_transform: this function is used for distance transform of a
%contour segment. We connect the two endpoints and together
%with the contour segment, a shape is generated.

%first step: connect the 


end

